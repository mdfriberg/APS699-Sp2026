# Conda environment file (conda-only)
# - Uses name/channels/dependencies fields for conda.
# - Avoid pip-only packages here to reduce conflicts.
# - If a package is pip-only, add a `pip:` subsection under dependencies.
# - Pip-only requirements use requirements.txt, not this YAML.
name: APS699-Sp2026-Test
channels:
  - conda-forge
dependencies:
  - python=3.11
  - xarray
  - pandas
  - cartopy
  - arm_pyart
  - satpy
  - metpy
  - boto
  - cfgrib
  - sharppy
